#include <stdio.h>
#include <string.h>


int main() {
    char c [100];
    char p [100];
    scanf("%s",c);

    scanf("%s",p);

    printf("You entered: %s and %s",c,p);

    return 0;
}